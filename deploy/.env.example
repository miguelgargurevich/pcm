# ================================
# Configuración de Producción - PCM
# Despliegue separado en Coolify
# ================================
# IMPORTANTE: Cambiar todos los valores antes de usar en producción

# Entorno
ASPNETCORE_ENVIRONMENT=Production
PORT=8080

# Base de Datos PostgreSQL
DB_HOST=84.247.167.43
DB_PORT=5432
DB_NAME=pcd_plataforma
DB_USER=admin
DB_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO

# JWT Authentication
JWT_SECRET=CAMBIAR_POR_CLAVE_SECRETA_MUY_LARGA_Y_SEGURA
JWT_ISSUER=PCM.API
JWT_AUDIENCE=PCM.Client
JWT_EXPIRATION=60

# Frontend público
VITE_API_URL=https://frontend.tu-dominio.com/api
CORS_ORIGIN_0=https://frontend.tu-dominio.com

# Opcionales
RECAPTCHA_SECRET_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=Plataforma de Cumplimiento Digital
SMTP_ENABLE_SSL=true
