# ================================
# Configuración de Producción - PCM
# ================================
# IMPORTANTE: Cambiar todos los valores antes de usar en producción

# Base de Datos PostgreSQL
POSTGRES_USER=pcm_user
POSTGRES_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO_123!
POSTGRES_DB=pcm_db

# JWT Authentication
JWT_SECRET=CAMBIAR_POR_CLAVE_SECRETA_MUY_LARGA_Y_SEGURA_DE_AL_MENOS_64_CARACTERES!
JWT_ISSUER=PCM
JWT_AUDIENCE=PCM-Users
JWT_EXPIRATION=60

# Supabase (opcional - dejar vacío si no se usa)
SUPABASE_URL=
SUPABASE_KEY=

# Entorno
ASPNETCORE_ENVIRONMENT=Production
